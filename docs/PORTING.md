当前地图仅为 `graypine_night`（灰松夜路）；哨站、沙漠、渡口及其附属场景已删除。后续完整验证只覆盖夜路单人与双人，旧版本记录中的其他地图及三/四人结果仅作历史参考。

# 移植说明

## v1.16.0 战斗与补给

当前规则、无限制输入验收口径及十种武器比较见 [COMBAT_REVISION.md](COMBAT_REVISION.md)。下文旧版本的恢复 50 生命、单只橄榄球、旧怪物预算及受限输入策略仅作历史记录。橄榄球本体 5000、护甲 4000，普通移动与玩家同为 4.2 米/秒；冲锋独立计算。

## v1.15.1 远处光照条纹

软件 Web 对照在夜路街道远处车身复现随转向变化的明暗条纹：只关闭太阳或只关闭路灯投影无效，关闭手电筒投影后消失。旧配置将与相机同位置的手电筒设为动态投影光，远处平面受到自身深度图的错误遮蔽。手电筒改为不计算阴影的照明，初始化与每次应用画质设置均保持关闭；月光和路灯继续按五档设置投影，光束位置、亮度、距离和贴墙衰减保持原值。手电筒不再贡献独立物体投影，这是本次取舍。

商铺地面另有独立的路灯自阴影条纹。夜路点光源改用封闭几何的背面生成阴影（shadow_reverse_cull_face），保留物体遮挡投影，避免表面自己遮蔽自己；本地图的地面、墙体、货架、树冠均使用有厚度的封闭网格。后续若添加单面纸片模型须复核投影。

原生断言覆盖全部五档及路灯背面投影；软件截图覆盖街道、建筑远墙、商铺墙面/地面和贴墙转向。QA 图库可显式重启旧手电筒投影或旧路灯剔除方式作负对照，正常游戏不提供此覆盖入口。空白车身面连续列和商铺地面连续行亮度变化用于检出旧条纹，不能替代人工查看截图。原生 GPU 与用户报错位置复核仍待验收，不能将 SwiftShader 结果表述为所有显卡问题均已消除。

## 基线与范围

- 河流使用原生 `river.gd` 构建河床、两侧缓坡和无实体碰撞的半透明流动水面，替换原有垂直河岸、水面碰撞与旧泡沫。保留原河道折线，岸顶高度 -0.05 米、河床 -0.9 米、水面 -0.28 米；岸顶半宽 2.6 米、平底半宽 0.7 米。两岸已有地面和道路边缘同步外移，与新缓坡接合，桥梁保留。
- 玩家与僵尸均可直接涉水，地面涉水移速为 70%；玩家起跳后保持正常水平速度，落回河床恢复涉水减速，无入水伤害或传送。橄榄球冲入水中转为涉水追击，在水中不蓄力冲刺。实体障碍仍阻挡寻路，河流格点可通行并记录涉水代价。
- 河床网格、物理碰撞与僵尸高度共享 `Data` 的河道剖面。CPU 命中姿态和 原生骨骼渲染共用敌人根变换，河中跳跃闪避按玩家与僵尸的相对高度判断。无窗口验证包含两岸步行出入、床面/缓坡碰撞采样、步行/跳跃与追击距离对照，以及真实浏览器输入涉水、起跳和过河。

- 玩家移动改用 `CharacterBody3D` 与半径 0.3 米、高 1.8 米的胶囊体，复用 `scenes/world.tscn` 烘焙保存的静态碰撞和 `river.gd` 生成的河床碰撞。`player_body.gd` 使用 `move_and_collide` 的扫掠结果和碰撞法线处理滑动、落地、斜坡与撞顶；显式传入位移以支持共享模拟的小步长，不依赖渲染帧时间。重力仍由角色脚本施加（18 米/秒²），起跳速度仍为 8.4 米/秒。
- `height` 现在表示脚底的真实世界高度，`grounded` 表示胶囊体检测到地面；站在高台上可以再次起跳，角色姿态和浏览器验收使用 `grounded`，不再假设落地高度必须为零。空中输入仍采用起跳时的移动量，视角转动仍影响水平方向。
- 单人/合作房主创建并驱动玩家物理节点，客户端只同步位置、高度、落地和涉水状态。节点在玩家离开、退出、重开和模拟器销毁时释放。僵尸仍采用共享二维地形寻路；角色胶囊体沿真实场景轮廓碰撞，敌人路径继续对实体障碍使用保守膨胀边界。击退也执行原生胶囊扫掠。
- 无窗口回归覆盖真实桥面通行、涉水、跳跃高度、空中禁止二段跳、高台落地与起跳、低天花板、墙面滑动、斜坡、击退阻挡及物理节点生命周期；ENet 2/4 人验证同时检查跳跃、落地快照和房主物理节点归属，不等同于 Steam 双账号验证。

- 橄榄球僵尸不再限制同时存活数量，按波次队列顺序和既有刷新速率生成，仍遵守安全出生点检查。

- 本地玩法调整：橄榄球僵尸在 0.35 秒蓄力结束时锁定方向，冲刺最长 4 秒；撞击造成 30 点伤害并沿冲刺方向击退最多 0.65 米（受地形、边界与敌人阻挡），保留受伤保护和跳跃闪避；撞墙眩晕 2 秒，扑空仍恢复 0.45 秒。由单人/合作房主共享模拟执行。

- 来源目录：`../Undead-Survivor`。
- 来源提交：`7775293140052ccf9f86ea86929bfbc8889a4a5b`，版本 0.7.7。
- 目标：Godot 4.5.2 Compatibility / GDScript；Steam 使用 GodotSteam 4.16 的 Godot 4.5 编译版。
- 原项目未修改；运行时不加载 WebView、不执行 JavaScript、不启动 Electron。

## 模块对应

| 原模块 | Godot 实现 | 迁移方式 |
| --- | --- | --- |
| `world.ts`、`terrainView.ts`、`geometry.ts` | `arena.gd`、`scenes/world.tscn` | 开发阶段转换原几何，按显式实例清单烘焙原生 ArrayMesh / MultiMesh 与静态碰撞；运行时直接实例化场景 |
| `terrain.ts`、`navigation.ts`、`player.ts` | `data.gd`、`arena.gd`、`river.gd`、`player_body.gd`、`simulation.gd` | 保留河道折线和桥梁，添加可涉水的实体河床与缓坡；角色原生胶囊扫掠，实体障碍 0.95 米导航膨胀，AStarGrid2D 路径与空间分区 |
| `config.ts`、`weapons.ts`、`enemyRoster.ts` | `rules.json`、`data.gd`、`simulation.gd` | 原数值提取、阶位池与刷新公式移植 |
| `firearm.ts`、`arsenal.ts` | `simulation.gd` | 弹匣、射速、装填、逐发装填、动作完成后切换 |
| `weapon.ts` | `convert-assets.mjs`、`weapon_view.gd` | 原 FBX 色板、轴向和骨骼修正；十款武器动作按 30 Hz 采样并导出 GLB |
| `sights.ts`、`SightOverlay.tsx` | `main.gd`、`interface.gd` | 真正的投影倍率、缩放灵敏度、原生瞄准镜绘制 |
| `zombies.ts` | `enemy_view.gd` | 43 个原始部件合并为共享蒙皮网格，每只僵尸 6 根骨骼驱动躯干、腿、手臂和盾牌；命中复用相同姿态 |
| `encounter.ts`、`movement.ts`、`spawn.ts` | `simulation.gd` | 单人/多人共用生存规则、攻击状态、精英技能、安全刷新、波次恢复 |
| `ballistics.ts`、`Game.ts` 攻击部分 | `data.gd`、`simulation.gd` | 确定性扇面散布、屏幕中心目标、枪口遮挡、逐部件命中、穿透/近战去重、按开火次数计命中 |
| `soundSynthesis.ts`、`audio.ts` | WAV、`sound.gd` | 原音乐、护甲和死亡采样保留；WebAudio 枪声/机械声等转为本地近似合成 |
| `blood.ts`、`armorEffects.ts` | `effects.gd` | 有上限的实例化血粒、护甲碎片和火焰 |
| `App.tsx`、`home.css`、菜单组件 | `interface.gd` | 原生 Control；主菜单采用当前源码的场景背景、左下标题、右侧模式列表；纸色设置与深色 HUD |
| `graphics.ts` | `data.gd`、`pixelation.gdshader` | 五档预设、单项设置和持久化；抗锯齿替换为 Godot MSAA |
| `leaderboard.ts` | `data.gd` | 本机前十，波次/击杀/时长排序，独立原生存档 |
| `CoopSession.ts`、`PartnerView.ts`、Steam bridge | `session.gd`、`partner_view.gd` | GodotSteam P2P / ENet；同一房主权威模拟、随机角色和配色、死亡观战、清波复活 |

## 运行约束

- 无法使用的刷怪入口不会减少配额，不累计突发补刷。
- 僵尸移动和冲锋均检查河道；冲锋蓄力时锁定方向。
- 每名玩家的存活状态、位置、弹匣与击杀只由房主更新；客户端字段经过白名单、类型、数值和序号检查。
- Steam 的大快照使用可靠 P2P，避免超过不可靠包 1200 字节限制；结束快照可靠发送。
- 仅从房主接受世界状态；仅从房间成员接受操作；解包不允许对象反序列化，限制包体和处理数量。
- 正常窗口由引擎自动绘制，失焦停止绘制；多人后台仍按固定物理步长模拟。仅软件 Web QA 保留集中限频绘制。
- 尸群使用轻量原生骨骼节点，不创建独立物理刚体；同类、同配色与护甲状态共享网格和材质。

## 验证与未验证项

标准 Godot 4.5.2 与 GodotSteam / Godot 4.5 均完成导入和 148 项无窗口运行检查，零失败。十款枪械、八类敌人和队友模型已完成实际 GPU 取图。双客户端和四客户端真实 ENet 进程之间的建房、加入、开始、输入、移动、射击和世界同步均已通过；最终四人回归的三个客户端各收到 89～90 次快照，全部进程退出无错误或警告。

历史实例动画提交中位耗时约 2.9 ms。v1.11.0 的实例动画在软件 WebGL 中出现姿态停留首帧，已改用原生骨骼：首版逐部件骨骼约 61 ms，合并为 6 根骨骼后，256 只姿态更新约 6.4 ms。绘制批次数量有所增加；这些是本机无窗口脚本测量，不代表完整 FPS，原生 GPU 性能待验收。

以下不作已通过声明：

- 两个独立 Steam 账号、不同电脑/网络的完整联网验收。GodotSteam 引擎与具体方法/信号已核对，房间及 P2P 实现已接入。
- 原生 Windows 图形窗口和跨电脑 Steam 完整运行体验。新增自动化流程会验证导出后的 EXE（无窗口模式），具体运行结果以 `artifacts/acceptance.json` 和 GitHub Actions 为准。
- 与原版逐像素一致。原几何和姿态保留，渲染器、阴影、字体栅格化、音频引擎和粒子表现有所不同。
- 原浏览器/Electron 的排行榜自动迁入。Godot 有独立存档，原存档保持原状。
- 原 Electron 与 Godot 客户端互联；两者协议和传输实现不同，应让全部队友使用同一 Godot 版本。

## 后续开发

修改 `assets/data/rules.json` 可调平衡；修改原项目后重新运行 `tools/convert-assets.mjs` 会覆盖该文件与生成资源。修改玩法请集中在 `simulation.gd`，保证单人与房主使用同一套规则。僵尸动作统一修改 `enemy_view.gd` 的 `pose_state`；骨骼渲染和逐部件命中都必须通过该函数，避免维护两套不同姿态。

工程关联独立远程仓库 `https://github.com/xcymm3/Undead-Survivor-Godot`。提交前执行完整自动验收与源码摘要门禁，使用简体中文 Conventional Commits 主题，不强制推送。流程与验收边界见 [AUTOMATION.md](AUTOMATION.md)。

## 原生组件后续改造

地图烘焙、HUD 控件、空间音频、显示快照插值和 InputMap 调整见 [NATIVE_COMPONENTS.md](NATIVE_COMPONENTS.md)。全量流程纳入夜路原生组件、单人整关与双人 ENet 整关验证。当前验收结果以 artifacts/acceptance.md 和源码摘要门禁为准，历史报告不代表当前源码。

## 2026-09-16 试玩反馈调整

- 三处补给站改为照明挂墙武器架，保持每人两把随机同级枪、只能换枪补弹和各携带一个手雷/医疗包的规则。
- 手雷引信 1.5 秒、半径 11 米，保留视线遮挡、中心 700 至边缘 150 伤害和不伤己方规则。
- 右键推击保留换弹进度，装填计时继续；切换武器仍按原规则中断。普通僵尸推击距离上限 3.2 米、扇形 160 度、硬直 2.4 秒、击退 2.8 米；重型敌人为 0.7 秒/0.9 米，橄榄球冲锋仍不可中断。三次连续推击的疲劳限制不变。中弹硬直不再覆盖更长的推击硬直。
- 僵尸移动播放有距离衰减的脚步声，停下不播放，近处最多八只参与并限频。背后实际受伤提供方向弧、后方遭袭提示和警示音。本地推击挥动和命中分开播放。受击保护由 0.3 秒增至 0.65 秒，单次伤害不变，给群体背袭后的转身和推击留出反应空间。
- 枪声 18 米、命中声 8 米，遮挡后有效距离为 45%；两者仍独立传播；只听见命中时沿来袭方向向前最多 8 米调查；同时听见枪口声音才能取得当次开枪位置，听者以追击速度的 35% 谨慎调查，看见玩家才全速追击。声音线索在最后一次听到后 12 秒失效，不会实时跟踪隔墙移动玩家，也不会把整图僵尸唤醒。无开枪来源的环境声音仍按声源点调查。
- 联机协议提升至 8，新增受击方向与挂墙高度的快照校验；只验收夜路单人/双人。软件 Web 画面、headless 功能与 ENet 本地双人可自动检查；原生 GPU、真人听感与 Steam 双账号仍需另验。


## 霰弹枪清群重设（v1.14.0）

泵喷 15 颗 × 40 伤害，横/纵散布 0.12/0.06；自动喷 17 颗 × 32，散布 0.105/0.05。5 米完整椭圆覆盖分别约 1.2×0.6 米、1.05×0.5 米。保持分层随机椭圆采样。8 米内全额伤害，8～24 米线性降至 20%，更远保持 20%。每颗最多命中两个目标，第二个承受 65%；铁桶、盾牌、橄榄球有效护甲命中阻挡后续穿透，实心地形始终截断射线。

近距普通僵尸受击踉跄 0.4 秒，同一目标冷却 1 秒；与现有推击剩余控制取最大值，不重设推击速度或位移。命中冲击音每枪一次，并延长命中准星反馈。其他枪械数值和正式随机补给保留。

`tools/validate-shotgun.gd` 提供独立全霰弹补给测试：B 枪替换泵喷、A 枪替换自动喷，初始主武器也换成泵喷，弹匣和备弹使用相同规则；仅在开局设置，之后不得补血、传送或动态赠送装备。单人与双人共享权威各运行 71245、71246 两个种子；当前受限角色按新要求取消转向速度上限，瞄准偏差减至 ±0.35°，取消额外射击反应等待，保留 1.5 秒路口观察（近敌时取消）；旧档为 120°/秒、±0.7°、0.25 秒等待，旧档成绩不能当作新档验收。霰弹枪瞄胸；沿用原角色近身切斧习惯，仍可使用手雷、推击和 C 级斧头；专项清空左轮弹药，避免用其他枪械兜底，并断言其他枪械发射数为零。泵喷交战打空再装填、近敌大于 8 米后可补弹。逐样本保存胜败、时长、装备射击数、生命和阶段时间，不把死亡样本删除或当成通关。双人此专项是同进程权威输入模拟，另有完整真实 ENet 回归；均不等于 Steam 双账号或真人验收。


## v1.15.0 装备与医疗动作

消防斧伤害仍为100、C级，有效命中距离从3.5米缩为7/3米（原值2/3）；挥击角度保持原值。左轮仍是6发弹巢、正常射击消耗弹药和换弹时长，但备弹无限（独立 infiniteReserve 规则，不是无限弹匣）。地图随机枪架不再产生左轮，开局副武器保留。

手雷携带上限3，补给点仍提供玩家数量的手雷，每次拾取1枚。HUD显示实际数量/3，第四枚不可拾取且不消耗世界库存。医疗包上限仍1。键盘、滚轮和权威装备判定共同拒绝空的4/5栏；最后一枚/最后一包使用后自动回主武器。网络协议升至9，允许0～3枚手雷。

3秒医疗保持恢复50生命及受击中断规则。执行治疗时自动蹲下，手伸向医疗包取绷带、包扎、固定、收包起身；同伴可观察第三人称动作。治疗与被治疗期间权威朝向冻结，鼠标只改变本地医疗观察镜头，可绕到正面，镜头仍进行墙体遮挡检查。完成或中断后回到角色原朝向的第一人称。本人第三人称医疗视角隐藏本人头顶姓名，避免起身时遮挡目标栏。

本次验收范围仍为 Night 单人、双人，不检验三/四人或其他地图。测试角色仅将持斧距离阈值关联到真实武器范围，并识别副武器无限备弹；全霰弹专项仍禁止左轮兜底。试验过提前拔斧、枪械优先、治疗安全窗口和进门优先推击，均未让两个固定受限样本全部通关，这些策略实验已撤回，失败结果保留在 artifacts/equipment-night-*-results.json。不得将功能通过等同整关平衡验收通过；最终整套检查结果见 artifacts/acceptance.json。

## 夜路积分怪群与独立橄榄球 Boss

以下为引入积分怪群时的初始预算，不是当前配置。旧输入档减量历史见 [NIGHT_BALANCE.md](NIGHT_BALANCE.md)；新输入档、当前数量、怪物组成影响和全部调参结果见 [NIGHT_BALANCE_V2.md](NIGHT_BALANCE_V2.md)。

开局区段预算46/44/68/42改为威胁积分；双人仍乘1.2并四舍五入。普通1、路障2、铁桶4、小鬼4、盾牌8、狂暴12。巨人从夜路名单排除；橄榄球不能通过积分池生成。开局各区至多50%积分换成特殊怪，其余全部补普通，名单循环先覆盖类型再重复，生成顺序使用权威随机数洗牌。树林预算足以包含五种特殊怪。位置仍在道路之外、房间/遮蔽物之后，开局待机；积分替换自然减少总人数，不能继续用旧180只断言代替预算核验。

树林12分、定时各10分增援保留，至多40%积分生成特殊怪。坚守三批仍各18分，但特殊积分上限2/3：第0秒以小鬼/路障/铁桶为池，第10秒以盾牌/路障/小鬼为池，第20秒以狂暴/铁桶/路障为池。积分不足跳过高价类型，余数补普通。正常积分批次仍按成组、遮挡、距离、可达和活动上限投放，剩余名单不丢失；日志记录budget/spent/roster，三者可核账。

坚守累计10秒只登记一次独立Boss债务，普通生成流程每步先尝试安全投放1只橄榄球，单人双人均1只。不扣威胁积分、组内名额或普通活动上限；Boss也不计入普通活动数量。所有入口受阻则延后且保留，不贴脸刷出、不穿墙生成；记录实际生成时刻及当时坚守时间。正常入口可用时应在10秒首次检查投放。重复交互不能重启或重复Boss。其现有4500生命/2000护甲及冲锋规则不变，不能把新增Boss当作已完成平衡验收。

验收覆盖积分守恒、各特殊类型、巨人排除、Boss第10秒/双人仅1只/独立容量/受阻保留/重复防护。保留原有受限通关门禁，不因新怪导致失败而放宽。软件截图检查六种特殊类型在夜路渲染，原生GPU与真人Boss体验待验收。

## 喷火器喷流重制（未提交）

火焰从碎屑 MultiMesh 分离：使用独立低面数体积粒子、无光照透明流动材质；每次 0.08 秒射击分时发射20个粒子，前进速度18→12米/秒，寿命0.76秒，无向下重力。喷流逐渐扩散、从黄白到橙红后淡出，少量尾部烟气轻微上浮；枪口增加短暂暖色无投影照明。粒子保留世界坐标、逐段检查地图碰撞，停止射击后自然消散，重开清空。关闭原喷火器的大块枪口闪光，其他枪械不变。独立火焰池上限640。

伤害保留36/次、0.08秒间隔、12米范围、100容量与原换弹时间；由中心细射线改为中心加两圈共13条喷流采样线，外圈斜率0.055（12米处偏移约0.66米），每条单独检查遮挡。共享命中集合，单次同一敌人最多承受36伤害和一次原有击退；仍可穿透敌人，不能穿墙。不增加燃烧持续伤害或额外控制。

原生专项检查覆盖喷流前进而不坠落、停火消散、表面阻挡、重复伤害去重、偏轴覆盖和穿透前排、墙后免伤。软件 Web 专项截图覆盖点火、持续喷射、侧视、余焰、消散和贴墙；属于受控画面检查，不等同真人操作或原生 GPU 验收。实际审查记录见 artifacts/visual-review.md，整套运行结果见 artifacts/acceptance.json。

贴墙保护：可见枪口穿入墙体时，喷流起点回退到权威遮挡前；枪口到阻挡面不足0.5米时不绘制挤在镜头前的火焰/暖光，其他近墙距离按距离降低暖光，避免全屏橙黄过曝。该保护仅影响显示，墙体阻挡伤害规则保持不变。

软件 Web 粒子池复用复查：缩短喷射时曾显示上一次长喷流的旧实例。池清空时重建实例缓冲，逐帧完整上传变换/颜色；图库冻结指定时刻并等待8帧绘制。复拍点火、持续和余焰阶段，核对权威粒子位置，避免把残留画面当作当前效果。

喷火光源在未喷射时完全隐藏，不仅将能量设为0，避免在 Compatibility 模式占用大面积地面等物体的灯光名额。光照旧缺陷对照在发现影响后重新验证，不降低检测阈值或跳过阴影测试。

## 三武器定向平衡（未提交，2026-09-17）

步枪伤害60→120，其余参数不变。自动霰弹枪弹容12→20、间隔0.36→0.24秒、每颗伤害32→48，维持17弹丸、散布、距离衰减、最多两目标与推击优先规则。狙击枪弹容5→15、间隔0.85→0.30秒、射击动画周期0.78→0.28秒，伤害280/爆头3倍不变；新增最多3目标穿透，依次100%/80%/64%，世界碰撞仍阻断。栓动标签保留，但这是明显偏快的游戏化拉栓速度，真人观感待验收。

tools/validate-target-weapons.gd 为固定71245/71246种子的无限制单人测试。初始武器及补给身份在开局替换，弹药遵循弹容×5备弹；之后只提交普通输入，不修改生命/敌人/弹药，不允许其他武器或手雷兜底。步枪和连喷所有枪械补给替换为各自测试枪；狙击枪只替换B级补给，保留实际A级供给，在存活且通过权威拾取取得A级主武器后结束，不能把它称为完整通关。

首轮狙击10发/0.5秒两局失败，完整结果保留 artifacts/target-weapons-round1.json。最终15发/0.3秒两局于67.60/61.05秒取得A级枪；步枪两局88.40/83.25秒通关，连喷两局92.95/106.50秒通关，报告 artifacts/target-weapons-acceptance.json。该标准没有继承旧受限角色150～270秒要求；也不代表真人难度或所有随机种子都已验证。完整回归以本次 artifacts/acceptance.json 为准。

## 五武器弹道散布（2026-09-17，未提交，专项尚未全部达标）

本轮只修改/测试步枪、P90、狙击、重机枪、左轮。Data.pellet新增aiming参数，Simulation.fire传入权威p.aim；单弹丸枪使用adsSpread收紧散布，保留确定性圆盘取样，不消耗导演随机序列。霰弹、普通手枪、喷火与消防斧保持不变，不在本轮验收范围。

最终腰射/ADS散布：步枪0.016/0.006，P90 0.030/0.010，狙击0.006/0.0015，重机枪0.045/0.018，左轮0.012/0.004。P90与重机枪因初拟0.05/0.02、0.06/0.025未稳定达标而回调，其余采用原建议。伤害、弹容、射速、怪物数量均未因本轮修改。

验证入口：tools/validate-headless.ps1 -Mode SpreadBallistics / SpreadWeapons。五枪分别进行2048发散布统计及腰射/ADS各8条实际权威射线检查，全部通过。实战使用无限制单人普通输入，固定71245/71246，不允许其他武器或手雷兜底；A级替换全部枪械供给，B级仅替换B级供给并要求活着实际拾取A级。左轮放在副武器槽，保留无限备弹，仅观察，不要求通关。测试策略仅提交普通开镜输入，无散布预测补偿；最终在最近活动威胁距玩家至少2.4米且射击时开镜，近身腰射，各样本均实际执行两种射击。

初始6米外开镜与逐轮回调记录保留在artifacts/spread-round1/2/3.json；最终报告artifacts/spread-weapons-acceptance.json。步枪两局87.70/83.25秒通关；重机枪112.15/111.35秒通关；狙击61.40/71.60秒活着取得A级；P90两局47.45/100.95秒失败，未到A级补给；左轮两局45.40秒失败，只作观察。P90伤害30→40补偿方案已向用户提出，尚无授权，未实施。

按用户明确限定仅测本轮五枪，没有运行包含未改武器的npm run verify；不能沿用上轮全绿报告或称本轮全绿，旧acceptance.json对当前源码已过期。没有提交、推送或打包发布。软件截图已审查，原生GPU/真人连续动态待验收。

### P90 伤害补偿复验（2026-09-17）

用户授权后仅将P90单发伤害30→40，并同步资源转换规则。腰射/ADS散布0.030/0.010、弹容45、射击间隔0.09秒、测试开镜策略与种子均不变。使用validate-headless.ps1 -Mode SpreadWeapons -P90Only复验；此选项复用同一验收代码，仅选择P90并写入独立报告，不覆盖五武器旧失败结果。

71245：62.00秒、112击杀、100生命，实际拾取A级枪；71246：57.75秒、110击杀、100生命，实际拾取A级枪。两局均执行腰射与ADS，没有其他枪械或手雷兜底，2/2满足B级生存至A级拾取标准，不宣称P90全程通关。报告artifacts/p90-40-acceptance.json；原五枪失败报告另存artifacts/spread-before-p90-40.json。结合上一轮步枪/重机枪/狙击达标记录，本次补齐P90目标；其他枪本轮未复测，旧全量acceptance.json仍不代表当前源码全绿。数值修改不涉及模型/渲染，本轮未重拍，原生GPU与真人观感待验收。暂不提交、推送或打包。

## 爬行僵尸（2026-09-17，未提交）

新增crawler：100生命/0护甲，与普通僵尸相同；追击随机速度为普通4.6～5.2的85%，即3.91～4.42米/秒。积分1。夜路预置与所有有限增援共用成功生成计数，每5个普通怪名额按4只normal+1只crawler生成，替换而非额外叠加；特殊怪和独立Boss不计入比例，安全条件阻塞时不提前消耗计数。碎片批次延续余数，防止小波次一直没有爬行怪。全局比例以原始生成类型计数，不包含铁桶/路障护甲掉落后的外观变化。

原生七骨骼姿态包含水平躯干、拖行双腿、交替前臂拉动、抬脸和贴地攻击；渲染骨骼与CPU射线盒使用同一pose_state。存活姿态头部低于0.95米，射击普通僵尸头部的1.7米水平线不命中，低位瞄准可爆头。爬行者保留普通怪受击/霰弹短踉跄与推击行为。普通僵尸等其他类型新增的头骨默认单位变换，不改变现有姿态。

原生断言包含积分、预置4:1、跨批次比例、受阻不计数、Boss不占比、生命速度、步态/攻击高度、低位爆头及渲染/CPU骨骼一致性；截图由真正headless Godot导出+headless Chromium软件渲染生成，原生GPU与真人实玩待验收。全量验收状态以本次报告为准，不以旧五武器或P90结果冒充新敌人平衡通过。

## 五套僵尸服饰（2026-09-17，未提交）

普通、爬行、路障、铁桶在权威spawn时从五套服饰等概率独立选择outfit 0～4：橙色反光背心工人（反光条/深灰袖/工作靴）、蓝色背带工装（背带/扣件/胸袋/卡其袖）、红色格纹衬衫（大格纹/门襟/牛仔裤）、白色医护服（绿色标记/口袋/绿色长裤）、深色西装（白衬衫/红领带/翻领/西裤）。头部与路障/铁桶护甲继续保留原有轮廓。

服饰编号通过独立RandomNumberGenerator由局种子与实体ID生成，不消耗导演/移动/战斗RNG。编号存于僵尸字典，自带于权威snapshot，同一实体在客户端、死亡及护甲脱落后保持相同服饰。其他特殊僵尸保持既有专属外观；无outfit字段的旧快照仍可按旧配色绘制。

服饰细节为薄表面布片，绑定躯干骨骼；衣袖/裤腿颜色随原骨骼动作，爬行、攻击与死亡均跟随相同姿态。按种类、服饰、护甲状态缓存网格。装饰不改变生命、伤害、速度、积分、生成比例或命中判定，未改变地图与其他武器。原生断言覆盖随机覆盖范围、导演RNG不变、snapshot序列化、护甲破坏/死亡保持服饰、特殊怪排除与网格缓存。

视觉计划按docs/VISUAL_QA.md检查四种怪×五套服饰、爬行左右步/侧面及攻击，共八张软件截图；headless Chromium/SwiftShader，不启动本机Godot图形窗口。原生GPU与真人连续观感待验收。全量自动状态以当前报告为准，不将既有爬行版单人战斗失败称为通过。

## 实体交互动画（2026-09-17）

夜路起点门改为两次 E 操作：首次拆除横向铁门闩，门闩下落、轻弹并留在地面；松开后再次按 E，门扇绕铰链旋转约 1.1 秒。门把手、钢带和提示牌跟随门扇。起点开门完成才允许出发。终点控制台点击拉动拉杆，30 秒坚守过程中锁栓逐渐退入；完成后门扇旋转打开。进屋按 E 关门，完整关闭后才结算；关闭途中门口有人、队友离开屋内或敌人进入则重新打开，不提前获胜。

换枪新增约 0.65 秒的物体移动：挂墙枪离架并移向持枪位置，旧枪从持枪位置抛落、翻转并留在地上。旧枪为废弃外观，不提供再次补弹；最近 24 次拾取保留有界视觉记录。医疗包和手雷拾取也有移入动作。没有新增第一人称手臂。拾取仍由权威端扣除物资，动画使用快照中的编号、起始时间、起止位置，不重复发放物资。新增枪械在换枪动画期间不可射击。

验证计划：原生检查两次按键、长按不连触发、开门完成前阻挡、完整关门后获胜、中途占门取消和拾取快照；headless Web 查看门闩、半开/全开门、控制台、关门与换枪起始/飞行/落地阶段，并执行真实键盘出发。按 docs/VISUAL_QA.md 实际逐图检查，原生 GPU、连续真人观感仍待授权环境验收。完整回归结果以本轮报告为准，不复用前一版本通过记录。

## 安全屋加固铁门美术重做（2026-09-17）

按用户参考图重做起点、终点门：厚钢门框与六角铆钉、凹入的暗色防护玻璃观察窗、七根竖栅及横向支撑、旧红漆分块护板、边缘锈迹/掉漆、斜向补强板、黄黑斜纹警示带和小型 EXIT 标识。使用原生分层网格，门的正反面都有结构；防护玻璃保留整扇门的阻挡规则。移除门上的巨大绿色提示牌，终点倒计时缩为控制台的小面板，详细交互指引仍由 HUD 显示。保留原门洞与碰撞尺寸、铰链方向、两次 E 拆闩开门、关门防夹和终点结算时机。

按 docs/VISUAL_QA.md，验收包含完整正面/斜面门、拆闩/旋转打开/关闭、控制台与真实键盘穿过起点；实际打开软件 Web 截图复查。原生 GPU 和真人连续观感仍待授权环境验证。完整回归结果另见当前 artifacts/acceptance.json，不能用专项截图代替整体验收。

## 发布前输入策略复查（2026-09-17）

无限制 QA 角色修复三项决策缺陷：近距离被围时允许使用为 Boss 预留的手雷；安全屋内仍有感染者时继续退让；双人均退离门扇转动区域后才停止移动并关门。仅改变普通输入策略，不修改游戏武器、生命、资源或刷怪预算，不降低通关断言。旧失败基线保留于 artifacts/release-baseline-night.json 和 artifacts/release-before-door-clearance-balance.json。全绿源码门禁及正式 Actions 成品仍是发布前提；专项执行成功不替代通关成功。

发布前视觉复查发现喷火器原始 MultiMesh 浮点缓冲在软件 Web 重用后出现持续火焰消失及绿色条带。改为引擎逐实例变换/颜色接口，复查持续喷射与侧视截图恢复正常，并增加可见火焰像素回归，防止仅以粒子数量判定通过。修复前后证据分别在 artifacts/browser-4215 和 artifacts/browser-4216；软件截图不替代原生 GPU 验收。

固定武器专项另修复两项输入污染：在救援提前返回前强制执行指定武器限制；禁止先产生斧子指令再强制换枪，避免近距离俯仰归零、丢失对爬行目标的瞄准。原生回归新增近身爬行目标仍使用指定枪械并向下瞄准的断言。此修复仅影响 QA 固定武器角色，不修改玩家武器数值或敌人预算。无效旧对照记录保留于 artifacts/release-before-rescue-weapons.json、artifacts/release-before-fixed-aim-comparison.json 和 artifacts/release-before-fixed-aim-targets.json。

## 追击攻击与精英移速（2026-09-18）

近战攻击保留各类型原有前摇。伤害帧前，僵尸沿实际追击速度继续推进，并以每秒120度上限朝已锁定目标转向；伤害帧后停止推进并完成原有收势。命中仍检查实际距离、攻击方向、实体遮挡和高度。由此速度快于玩家的敌人可在直线追逐中命中，玩家横移、推击、地形阻挡仍能造成挥空。普通站立僵尸在前摇推进时混合腿部步态与上半身挥击，CPU命中盒与GPU骨骼继续共用enemy_view姿态。

陆地追击速度：普通/路障/铁桶/橄榄球4.60～5.20米/秒；爬行3.91～4.42；小鬼为原1.08倍率再提高20%，即5.96～6.74；持盾3.59～4.06；狂暴僵尸常态4.60～5.20、狂暴态固定7.2；巨人3.13～3.54。涉水追击统一乘0.7。橄榄球冲锋固定12米/秒，入水仍取消冲锋。

原生断言覆盖各精英实际位移、橄榄球有甲/破甲追击与冲锋速度、直线后退受击、横向闪避挥空、地形和原有命中检查。按docs/VISUAL_QA.md复查攻击前摇推进的腿部步态、上半身挥击和CPU/GPU姿态一致性；软件Web截图与完整无窗口回归另记录于本次报告，原生GPU和真人连续手感仍待隔离环境验收。
